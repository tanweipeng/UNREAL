using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Zone_Solved : MonoBehaviour
{
    public void TTDI_solved()
    {
        if(database.clueValue[0]
            && database.clueValue[1]
            && database.clueValue[2]
            && database.clueValue[3]
            && database.clueValue[4]
            && database.clueValue[5]
            && database.clueValue[6]
            && database.clueValue[7]
            && database.clueValue[8]
            && database.clueValue[9]
            && database.clueValue[10])
        {

        }
    }
}
